name: deploy

on:
  push:
    branches: [main]

jobs:
  validate-harness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npx sdlc-harness validate

  deploy:
    needs: validate-harness
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add the real build/deploy steps for {{projectName}} here"
