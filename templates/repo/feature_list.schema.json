{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sdlc-harness feature_list.json",
  "type": "object",
  "required": ["project", "schema_version", "milestones", "features"],
  "properties": {
    "project": { "type": "string" },
    "schema_version": { "type": "string" },
    "last_updated": { "type": "string" },
    "rules": {
      "type": "object",
      "properties": {
        "wip_limit_per_owner": { "type": "integer", "minimum": 1 },
        "single_active_feature": {
          "type": "boolean",
          "description": "Deprecated alias for wip_limit_per_owner: 1 (true) or unlimited (false). Ignored if wip_limit_per_owner is set."
        },
        "passing_requires_evidence": { "type": "boolean" },
        "do_not_skip_verification": { "type": "boolean" },
        "feature_should_fit_one_session": { "type": "boolean" },
        "passing_is_monotonic": { "type": "boolean" },
        "dependencies_must_be_passing_before_start": { "type": "boolean" }
      }
    },
    "rule_exceptions": { "type": "object" },
    "status_legend": { "type": "object" },
    "milestones": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "objective"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "objective": { "type": "string" }
        }
      }
    },
    "features": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "milestone", "behavior", "status", "dependencies", "verification"],
        "properties": {
          "id": { "type": "string" },
          "milestone": { "type": "string" },
          "area": { "type": "string" },
          "title": { "type": "string" },
          "behavior": { "type": "string" },
          "owner": {
            "type": "string",
            "description": "Agent or developer this feature is assigned to. Features with no owner share one implicit bucket, so a repo that never sets owner keeps the old single-active-feature behavior."
          },
          "risk": {
            "enum": ["low", "medium", "high"],
            "description": "Informational signal for which upstream stages (architecture, user stories, milestone planning) are worth doing in full before implementation. Not enforced by validate."
          },
          "status": { "enum": ["not_started", "in_progress", "blocked", "passing"] },
          "dependencies": { "type": "array", "items": { "type": "string" } },
          "verification": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["type", "command", "expected"],
              "properties": {
                "type": { "type": "string" },
                "command": { "type": "string" },
                "expected": { "type": "string" }
              }
            }
          },
          "evidence": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["kind", "result", "recorded_at"],
              "properties": {
                "kind": { "type": "string" },
                "command": { "type": "string" },
                "result": { "type": "string" },
                "recorded_at": { "type": "string" }
              }
            }
          },
          "source_refs": { "type": "array", "items": { "type": "string" } },
          "implementation": {
            "type": "object",
            "properties": {
              "files": { "type": "array", "items": { "type": "string" } },
              "commits": { "type": "array", "items": { "type": "string" } }
            }
          },
          "blocker": { "type": ["string", "null"] },
          "notes": { "type": "string" }
        }
      }
    }
  }
}
